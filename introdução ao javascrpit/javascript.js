let frutas = "manga"; 

// TIPOS DE DADOS DE UMA VARIAVEL   
// para dados de texto, o tipo se torna string

let nome = 'carlos'

// para numeros inteiros usamos o tipo integers

let idade = 20;

// já para os números quebrados usamos o tipo floats

let contabancária = 0.79;

// booleano é o que define se algo é verdadeiro ou falso por exemplo:

let carlosbranco = false;
let carlosmoreno = true;

// arrays funciona como uma listagem dentro da variavel usando colchetes []:

let mercado = ["arroz, feijão, legumes, carne"]

// para ascessar esses valoeres cada item da listagem reporesenta um número começando do primeiro intem sendo reporesentado pelo 0 e assim sucessivamente.

frutas [0];

// objeto é para indicar especificamente cada valor dado à variavel, por exemplo:

let computador = {configurações:'4 de ram ssd de 500 e processador de 2gb', cor:'preta', perifericos:'mause gamer, teclado gamer e fone gamer'}

// para acessar a variavel se escreve asssim:

computador.configurações

